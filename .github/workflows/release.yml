name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  macos:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6

      - name: Install Mise toolchain
        uses: jdx/mise-action@v3
        with:
          version: 2026.7.7
          install: true
          cache: true

      - name: Verify tag version
        run: |
          expected_version="${GITHUB_REF_NAME#v}"
          actual_version=$(mise run run -- --version)
          if [[ "$actual_version" != "$expected_version" ]]; then
            echo "Tag $GITHUB_REF_NAME does not match binary version $actual_version" >&2
            exit 1
          fi

      - name: Test
        run: mise run test

      - name: Build universal macOS binary
        run: |
          mise run build:release:macos
          mkdir -p artifacts
          tar -C staging/macos -czf "artifacts/vid-${GITHUB_REF_NAME}-macos-universal.tar.gz" vid

      - uses: actions/upload-artifact@v7
        with:
          name: macos
          path: artifacts/*.tar.gz
          if-no-files-found: error

  linux:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6

      - name: Install Mise toolchain
        uses: jdx/mise-action@v3
        with:
          version: 2026.7.7
          install: true
          cache: true

      - name: Install matching Static Linux SDK
        run: mise run sdk:install:linux

      - name: Build static Linux binaries
        run: |
          mise run build:release:linux
          mkdir -p artifacts
          tar -C staging/linux-x86_64 -czf "artifacts/vid-${GITHUB_REF_NAME}-linux-x86_64.tar.gz" vid
          tar -C staging/linux-aarch64 -czf "artifacts/vid-${GITHUB_REF_NAME}-linux-aarch64.tar.gz" vid

      - uses: actions/upload-artifact@v7
        with:
          name: linux
          path: artifacts/*.tar.gz
          if-no-files-found: error

  publish:
    needs: [macos, linux]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create checksums
        working-directory: artifacts
        run: sha256sum vid-*.tar.gz > SHA256SUMS

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release create "$GITHUB_REF_NAME" \
              --repo "$GITHUB_REPOSITORY" \
              --title "$GITHUB_REF_NAME" \
              --generate-notes
          fi
          gh release upload "$GITHUB_REF_NAME" \
            artifacts/vid-*.tar.gz \
            artifacts/SHA256SUMS \
            --repo "$GITHUB_REPOSITORY" \
            --clobber
